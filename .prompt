MAGENTA=$(tput setaf 5)
BLUE=$(tput setaf 4)
GREEN=$(tput setaf 2)
WHITE=$(tput setaf 7)
YELLOW=$(tput setaf 3)

function parse_git_dirty() {
	[[ $(git status 2> /dev/null | tail -n1) != "nothing to commit (working directory clean)" ]]
}

function parse_git_branch() {
	git branch --no-color 2> /dev/null | sed -e '/^[^*]/d' -e "s/* \(.*\)/\1$(parse_git_dirty)/"
}

# PS1="\[\e]2;$PWD\[\a\]\[\e]1;\]$(basename "$(dirname "$PWD")")/\W\[\a\]\[$MAGENTA\]\u \[$WHITE\]at \[$BLUE\]\h \[$WHITE\]in \[${GREEN}\]\w \[${GREEN}\]\[$WHITE\]\$([[ -n \$(git branch 2> /dev/null) ]] && echo \"on \")\[$YELLOW\]\$(parse_git_branch) \[$WHITE\]\n\$ "

PS1="\[\e]2;$PWD\[\a\]\[\e]1;\]$(basename "$(dirname "$PWD")")/\W\[\a\]\[${GREEN}\]\w \[${GREEN}\]\[$WHITE\]\$([[ -n \$(git branch 2> /dev/null) ]] && echo \"on \")\[$YELLOW\]\$(parse_git_branch) \[$WHITE\]\n\$ "